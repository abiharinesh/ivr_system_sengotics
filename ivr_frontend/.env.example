# Copy to .env and set your key. Used by Flutter web for Google Maps.
GOOGLE_MAPS_API_KEY=your_google_maps_js_api_key_here

# Optional: API origin for Dio (all platforms). If unset, web uses the live server; mobile/desktop debug uses http://localhost:3000.
# API_BASE_URL=https://ivr-system-sengotics.vercel.app
